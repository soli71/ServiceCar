<!-- Views/Cars/Index.cshtml -->
@using OilChangeApp.Data
@model IEnumerable<Car>

@section Styles {
    <style>
        .card {
            margin-bottom: 20px;
        }
    </style>
}

@{
    ViewData["Title"] = "لیست خودروها";
}

<div class="container">
    <h2 class="mb-4">@ViewData["Title"]</h2>

    <p>
        <a asp-action="Create" class="btn btn-success">
            <i class="fas fa-plus"></i> ایجاد خودروی جدید
        </a>
    </p>

    <div class="row">
        @foreach (var car in Model)
        {
            <div class="col-md-4">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">@car.Name</h5>
                        <div class="btn-group" role="group">
                            <a asp-action="Details" asp-route-id="@car.Id" class="btn btn-info btn-sm">
                                <i class="fas fa-info-circle"></i> جزئیات
                            </a>
                            <a asp-action="Edit" asp-route-id="@car.Id" class="btn btn-warning btn-sm">
                                <i class="fas fa-edit"></i> ویرایش
                            </a>
                            <a asp-action="Delete" asp-route-id="@car.Id" class="btn btn-danger btn-sm">
                                <i class="fas fa-trash"></i> حذف
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
</div>
